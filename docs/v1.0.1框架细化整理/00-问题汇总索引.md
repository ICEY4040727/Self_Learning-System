# v1.0.1 框架细化整理 — 问题汇总

> 对 Self_Learning-System 项目代码进行全面审查，找出冗余、重复、逻辑不清的部分。
> 生成时间：2026-04-14

## 问题总览

| # | 文件 | 问题类型 | 严重程度 |
|---|------|----------|----------|
| 01 | stage_map 在 save.py 和 archive.py 中重复定义 | 重复定义 | 中 |
| 02 | skill-cowork 中 README 和 CONTRIBUTING 的双语内容完全冗余 | 内容冗余 | 低 |
| 03 | _default_relationship 在 models.py 和 archive.py 中重复定义 | 重复定义 | 中 |
| 04 | character_sprites 是 sage_sprites 和 traveler_sprites 的冗余超集 | API 冗余字段 | 中 |
| 05 | start_learning 两条路径的返回字典几乎完全重复 | 函数内代码重复 | 中 |
| 06 | save.py 遗留接口与新接口并存代码膨胀 | 遗留代码 | 高 |
| 07 | archive.py 中 _ensure_world_knowledge 是空操作但仍被调用 | 死代码 | 中 |
| 08 | 前端关系阶段标签在 types 和 constants 中重复定义 | 重复定义（前端） | 中 |
| 09 | main.py 残留 HOT RELOAD TEST 注释 | 调试残留 | 低 |
| 10 | learning.py 中 tool_confirm 是占位接口但已暴露在生产路由 | 占位代码 | 中 |
| 11 | archive.py 中 _get_world_sages 是冗余的向后兼容包装 | 死代码 | 低 |
| 12 | save.py 中 ChatMessage 被重复导入 | 冗余导入 | 低 |
| 13 | user-profile 两个端点响应格式不一致 | API 不一致 | 中 |
| 14 | 活跃 session 查询逻辑在 learning.py 中重复三次 | 重复查询逻辑 | 高 |
| 15 | 角色模板数据前后端重复定义且 key 不匹配 | 重复定义 + 潜在 Bug | 高 |
| 16 | buildTraitsPayload 是无意义的恒等包装函数 | 冗余函数 | 低 |
| 17 | 关系阶段推算存在两套完全不同的算法 | 逻辑冲突 | 高 |
| 18 | 服务层自管数据库连接绕过 FastAPI 依赖注入 | 架构问题 | 高 |
| 19 | SAVE_DIR 硬编码相对路径依赖 CWD | 硬编码 / 配置不当 | 中 |
| 20 | ChatMessage 查询在 save.py 中重复 6 次（#14 加剧） | 重复查询逻辑 | 高 |
| 22 | 前端页面背景图片硬编码导致视觉不统一 | 前端硬编码 / UX | 中 |

## 按严重程度分组

### 高（需优先修复，共 6 项）
- 06 — save.py 遗留接口 150 行冗余代码（文件已膨胀至 888 行）
- 14 — 活跃 session 查询重复 3 次，含 1 次不必要的数据库查询
- 15 — 前后端角色模板 key 不匹配（英文 vs 中文），模板查找永远 fallback
- 17 — 关系阶段存在两套完全不同的推算算法，结果可能矛盾
- 18 — 服务层自管数据库连接，绕过 FastAPI 依赖注入，存在资源泄漏风险
- 20 — ChatMessage 查询在 save.py 中重复 6 次（#14 的加剧，Issue #207 后从 3 次增至 6 次）

### 中（建议修复，共 11 项）
- 01 — stage_map 在 save.py 和 archive.py 中重复定义
- 03 — _default_relationship 在 models.py 和 archive.py 中重复定义
- 04 — character_sprites 是 sage_sprites 和 traveler_sprites 的冗余超集
- 05 — start_learning 两条路径的返回字典几乎完全重复
- 07 — _ensure_world_knowledge 是空操作但仍被调用
- 08 — 前端关系阶段标签在 types 和 constants 中重复定义
- 10 — tool_confirm 占位接口已暴露在生产路由
- 13 — User Profile 两个端点响应格式不一致
- 19 — SAVE_DIR 硬编码相对路径依赖 CWD，部署时可能解析到错误目录
- 22 — 前端页面背景图片硬编码（来源/定位/渐变不统一 + 切换跳变）

### 低（清理优化，共 5 项）
- 02 — skill-cowork 双语文档冗余
- 09 — HOT RELOAD TEST 调试注释残留
- 11 — _get_world_sages 未被调用的死代码
- 12 — ChatMessage 重复导入
- 16 — buildTraitsPayload 无意义的恒等函数

## 按问题类型分组

| 类型 | 问题编号 | 数量 |
|------|----------|------|
| 重复定义（常量/映射） | 01, 03, 08, 15 | 4 |
| 代码/逻辑重复 | 05, 14 | 2 |
| 死代码/遗留代码 | 06, 07, 09, 11 | 4 |
| API 一致性 | 04, 13 | 2 |
| 架构问题 | 18 | 1 |
| 逻辑冲突 | 17 | 1 |
| 冗余函数/导入 | 12, 16 | 2 |
| 内容冗余 | 02 | 1 |
| 占位代码 | 10 | 1 |
| 前端硬编码 / UX | 22 | 1 |
