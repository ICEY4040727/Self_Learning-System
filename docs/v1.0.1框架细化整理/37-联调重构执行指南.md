# v1.0.1 联调重构执行指南

> **状态**: APPROVED — 三方讨论已确认（含设计层重构），可执行
> **⚠️ Reviewer 注意事项**: 见下方 Reviewer 观察点
> **决策来源**: [前后端联调重构计划-draft](./前后端联调重构计划-draft.md) | [设计层重构计划](./设计层重构-draft.md)
> **日期**: 2026-04-14（设计层整合更新）
> **执行者**: Creator
> **审阅者**: Reviewer

---

## 确认的决策

### Reviewer 观察点（Creator 执行前务必注意）

> **来源**: Reviewer 对 37-联调重构执行指南的审阅反馈

1. **DD18 max_tokens ⚠️ 待澄清** — 建议在 Phase 6 执行前与 Owner 确认意图。Reviewer 认为修复不会降低体验，建议最小改动修复。
2. **Phase 1.5 风险最高（中高）** — TeacherPersona 合并涉及核心模型变更，需要 Alembic 迁移 + 回滚脚本，建议特别仔细执行。
3. **Phase 3 工作量最大（3-4天）** — 整合了联调 + 6 个设计层决策（DD2/3/7/8/9/11），建议拆为 3 个 PR（后端 + 前端常量 + 前端组件）。
4. **§1.5 引用文件路径已修正** — 原文引用 `backend/api/routes/character.py`，该文件不存在。角色创建端点实际在 `backend/api/routes/archive.py` 中。已在下方修正。

---

### 联调重构决策

| 决策 | 结论 | 条件 |
|------|------|------|
| D1: save.py 遗留接口 | ✅ 立即删除（选 A） | 需文档记录未迁移项 |
| D2: Store vs API | ✅ Store 模式（选 A） | 需补齐 characterApi 的 5 个缺失方法 |
| D3: 角色模板 key | ✅ 统一为 `socrates` | — |
| D4: tool_confirm | ✅ 删除（选 A） | — |
| D5: 执行顺序 | ✅ 顺序执行，不并行 | Phase 0→1→2→3→4 |

### 设计层重构决策（22 项）

> 来源：[设计层重构计划](./设计层重构-draft.md)

| 决策 | 结论 | 条件/备注 |
|------|------|-----------|
| DD1: TeacherPersona | ✅ 完全删除，合并到 Character（Owner 选 D） | Reviewer 附条件：is_active 语义文档化、迁移独立 PR+回滚脚本、验收 grep 零结果 |
| DD2: 创建流程 | ✅ 统一到 SageCreateFlow（选 A） | 删除 CreatePersonaModal |
| DD3: 模板 key | ✅ 统一为 `socrates` | 三方一致 |
| DD4: Prompt 策略 | ✅ 基础规则+模板扩展（选 C） | 三方一致 |
| DD5: template_name | ✅ 持久化到 Character，影响 Prompt（选 B） | 与 DD4=C 联动 |
| DD6: speech_style | ✅ 保持 Text，前端 join（选 B） | 三方一致 |
| DD7: TRAIT_SLIDERS | ✅ 抽取到 constants/traitSliders.ts（选 B） | 三方一致 |
| DD8: greeting | ✅ 直接删除 | Owner 决定，Reviewer 同意 |
| DD9: tags/traits 命名 | ✅ 统一术语：标签→tags，数值→trait_params | 三方一致，尽快改 |
| DD10: 颜色系统 | ✅ 统一为 hex + 加 theme_color 列（低优先级） | Phase 4 处理 |
| DD11: 模板列表 | ✅ 保留 5 个模板，每个需完整 style_extensions | Reviewer 要求 |
| DD12: knowledge_svc | ✅ 重命名为 db | 三方一致，尽快修复 |
| DD13: prev_emotion | ✅ 🔴 高优先级修复（1 行 bug） | 三方一致，尽快修复 |
| DD14: Seed Memory | ✅ 不去重（per-session 语义） | 需文档化语义，建议加 source_session_id |
| DD15: 召回计数 | ✅ 修复，检索后调用 update | 三方一致 |
| DD16: fact_type 枚举 | ✅ 统一为枚举 | 三方一致 |
| DD17: Adapter 重复 | ✅ 提取公共基类 | 需先完成 DD1 迁移再处理 |
| DD18: max_tokens | ⚠️ 待澄清 Owner 意图 | Reviewer：修复不会降低体验，建议最小改动修复 |
| DD19: ChatMessage 职责 | ✅ 统一到 LearningEngine | 三方一致 |
| DD20: FSRS 接入 | ✅ 高优先级但独立立项 | 联调完成后单独处理 |
| DD21: 端点错放 | ✅ 低优先级，路由重组时迁移 | 三方一致 |
| DD22: 隐含副作用 | ✅ 低优先级，至少加注释文档化 | 三方一致 |

---

## 问题覆盖验证

### 已解决（无需操作）

| # | 问题 | PR |
|---|------|-----|
| 03 | 版本号硬编码 | #221 |
| 05 | start_learning 返回重复 | #219 |
| 08 | STAGE_LABELS 重复 | #220 |
| 13 | user-profile 格式不一致 | #220 |
| 14 | session 查询重复 | #219 |
| 15 | 角色模板 key 不匹配 | #220 |
| 19 | SAVE_DIR 硬编码 | #221 |

### 待解决（27+22 项）→ 全部分配到 Phase

#### 代码层问题（#01-#36）

| # | 问题 | Phase | 指南章节 |
|---|------|-------|---------|
| 01 | stage_map 重复（save.py） | Phase 1 | §1.1 |
| 02 | Limiter 实例重复 | Phase 3 | §3.3 |
| 04 | character_sprites 冗余（legacy alias） | Phase 3（→ #36） | §3.5 |
| 06 | save.py 遗留接口 | Phase 1 | §1.1 |
| 07 | _ensure_world_knowledge 空操作 | Phase 0 | §0.2 |
| 09 | HOT RELOAD 注释 | Phase 0 | §0.4 |
| 10 | tool_confirm 占位 | Phase 3 | §3.2 |
| 11 | _get_world_sages 死代码 | Phase 0 | §0.3 |
| 12 | ChatMessage 重复导入 | Phase 1 | §1.1 |
| 16 | buildTraitsPayload 恒等函数 | Phase 2 | §2.3 |
| 17 | 两套关系算法（死代码） | Phase 0 | §0.1 |
| 18 | 自管 DB 连接 | Phase 3 | §3.1 |
| 20 | ChatMessage 查询重复 6 次 | Phase 1 | §1.1 |
| 22 | 背景图片硬编码 | Phase 4 | §4.3 |
| 23 | MOCK 重复 | Phase 2 | §2.4 |
| 24 | DEBUG 日志残留 | Phase 2 | §2.1 |
| 25 | page-fade 冲突 | Phase 2 | §2.5 |
| 26 | 组件文件名重复 | Phase 4 | §4.4 |
| 27 | alert vs toast | Phase 4 | §4.2 |
| 28 | 冗余资源 | Phase 2 | §2.2 |
| 29 | API 模块未使用 | Phase 4 | §4.1 |
| 30 | types 混合常量 | Phase 3 | §3.6 |
| 31 | 角色模板重复 | Phase 3 | §3.7 |
| 32 | worldStore 未使用 | Phase 4 | §4.1 |
| 33 | error-toast 重复 | Phase 3 | §3.8 |
| 34 | save.py stage_map 未统一（#01 遗留） | Phase 1 | §1.1 |
| 35 | client.ts refresh 兼容代码 | Phase 2 | §2.6 |
| 36 | character_sprites legacy alias（#04 遗留） | Phase 3 | §3.5 |

#### 设计层问题（DD1-DD22）

| 决策 | 问题 | Phase | 指南章节 |
|------|------|-------|---------|
| DD13 | prev_emotion 始终 None（🔴 bug） | Phase 0 | §0.5 |
| DD1 | TeacherPersona 合并到 Character | Phase 1.5 | §1.5 |
| DD12 | knowledge_svc 重命名 | Phase 1.5 | §1.5 |
| DD9 | tags/traits 术语统一 | Phase 3 | §3.7 |
| DD2 | 统一到 SageCreateFlow | Phase 3 | §3.7 |
| DD3 | 模板 key 统一 socrates | Phase 3 | §3.7 |
| DD7 | TRAIT_SLIDERS 抽取 | Phase 3 | §3.7 |
| DD8 | 删除 greeting | Phase 3 | §3.7 |
| DD11 | 模板列表统一（5 个） | Phase 3 | §3.7 |
| DD5 | template_name 持久化 | Phase 5 | §5.1 |
| DD4 | Prompt 基础规则+扩展 | Phase 5 | §5.2 |
| DD14 | Seed Memory 文档化语义 | Phase 6 | §6.1 |
| DD15 | 召回计数修复 | Phase 6 | §6.2 |
| DD16 | fact_type 枚举统一 | Phase 6 | §6.3 |
| DD17 | Adapter 基类提取 | Phase 6 | §6.4 |
| DD18 | max_tokens 修复（⚠️ 待澄清） | Phase 6 | §6.5 |
| DD19 | ChatMessage 职责统一 | Phase 6 | §6.6 |
| DD20 | FSRS 接入（独立立项） | Phase 7 | §7.1 |
| DD10 | 颜色系统统一 | Phase 4 | §4.3 |
| DD21 | 端点错放 | Phase 6 | §6.7 |
| DD22 | 隐含副作用文档化 | Phase 6 | §6.8 |

✅ **验证通过**: 7 已解决 + 27 代码层 + 22 设计层 = **56 项**，全部覆盖。

---

## Phase 0 — 后端死代码清除 + Bug 修复

> **预估工作量**: 0.5 天 | **风险**: 极低 | **PR 建议**: 单个 PR

### §0.1 删除两套关系算法中的死代码（#17）

**背景**: `dynamic_analyzer.py` 中曾有 `update_relationship_stage` 方法，属于"算法 B"。经 Reviewer 确认该方法是死代码（零调用），但尚未删除。

**操作**:
1. 打开 `backend/services/dynamic_analyzer.py`
2. 确认 `update_relationship_stage` 方法是否存在
3. 如果存在：删除整个方法（约 46 行）
4. 全局搜索确认无调用：`grep -r "update_relationship_stage" backend/ --include="*.py"`
5. 确认 `relationship.py` 中是否有类似的死代码算法

**验收**: `grep` 零结果 + `pytest backend/tests/` 通过

### §0.2 删除 _ensure_world_knowledge 空操作（#07）

**文件**: `backend/services/archive.py`

**操作**:
1. 搜索 `_ensure_world_knowledge` 函数定义
2. 确认函数体为空操作（pass 或 return None）
3. 删除函数定义
4. 全局搜索所有调用点：`grep -r "_ensure_world_knowledge" backend/ --include="*.py"`
5. 删除所有调用点（预计 3 处）

**验收**: `grep` 零结果 + `pytest` 通过

### §0.3 删除 _get_world_sages 死代码（#11）

**文件**: `backend/services/archive.py`

**操作**:
1. 搜索 `_get_world_sages` 函数定义
2. 确认是冗余的向后兼容包装
3. 删除函数定义
4. 全局搜索确认无调用：`grep -r "_get_world_sages" backend/ --include="*.py"`

**验收**: `grep` 零结果 + `pytest` 通过

### §0.4 删除 HOT RELOAD 注释（#09）

**文件**: `backend/main.py`

**操作**:
1. 搜索 `# HOT RELOAD TEST` 或类似注释
2. 删除该注释行及相关调试代码（如有）

**验收**: `grep -r "HOT RELOAD" backend/ --include="*.py"` 零结果

### §0.5 修复 prev_emotion 始终 None（DD13 🔴）

**文件**: `backend/services/learning_engine.py`

**操作**:
1. 找到 `"prev_emotion": None` 硬编码（约 L157）
2. 找到下方查询 `last_user_msg.emotion_analysis` 的代码（约 L170-177）
3. 将 `prev_emotion: None` 改为使用查询结果
4. 删除 `pass` 语句

**验收**: `prev_emotion` 使用实际的 `emotion_analysis` 值，`pytest` 通过

### Phase 0 验收清单

- [ ] `pytest backend/tests/` 全部通过
- [ ] `grep -r "update_relationship_stage\|_ensure_world_knowledge\|_get_world_sages\|HOT RELOAD" backend/ --include="*.py"` 零结果
- [ ] `python -c "import ast; ast.parse(open('backend/services/dynamic_analyzer.py').read()); ast.parse(open('backend/services/archive.py').read()); ast.parse(open('backend/main.py').read()); print('OK')"` 通过
- [ ] `prev_emotion` 不再硬编码为 None

---

## Phase 1 — 后端 save.py 重构

> **前置条件**: Phase 0 已合并 | **预估工作量**: 1-2 天 | **风险**: 中 | **PR 建议**: 单个 PR

### §1.1 save.py 综合重构（#01, #06, #12, #20, #34）

**文件**: `backend/api/routes/save.py`（当前约 888 行）

**步骤 1 — 删除遗留接口（#06）**

1. 识别遗留 save 接口：
   - `POST /save` — 创建存档（旧）
   - `GET /save` — 列出存档（旧）
   - `GET /save/{id}` — 获取单个存档（旧）
   - `DELETE /save/{id}` — 删除存档（旧）
2. 确认前端无调用：`grep -r "/save" frontend/src/ --include="*.ts" --include="*.vue"`
3. ⚠️ **Owner 要求**: 如发现未迁移的调用，需在 PR 描述中明确记录
4. 删除上述端点及其辅助函数 → 预计减少 ~150 行

**步骤 2 — 提取查询辅助函数（#20）**

1. 搜索 save.py 中所有 `db.query(ChatMessage)` 调用
2. 识别重复模式：
   - 按 session_id 查询消息列表 → 提取 `_get_session_messages(db, session_id)`
   - 按 session_id 统计消息数 → 提取 `_count_session_messages(db, session_id)`
3. 替换所有重复查询为新辅助函数

**步骤 3 — 清理重复导入（#12）**

1. 检查 save.py 文件头部和函数内部的 `ChatMessage` 导入
2. 保留文件头部的导入，删除函数内部的重复导入

**步骤 4 — 统一 stage_map（#01/#34）**

1. 搜索 save.py 中的 `stage_map` 或 `STAGE_MAP` 定义
2. 替换为从 `backend/core/config.py` 或 `backend/services/relationship.py` 导入的 `RELATIONSHIP_STAGE_LABELS`
3. 如果 `RELATIONSHIP_STAGE_LABELS` 尚不存在，在合适位置创建

**验收**:
- `pytest backend/tests/` 通过
- `wc -l backend/api/routes/save.py` < 650 行
- `grep -c "db.query(ChatMessage)" backend/api/routes/save.py` ≤ 2（仅在辅助函数内）

### Phase 1 验收清单

- [ ] `pytest backend/tests/` 全部通过
- [ ] save.py < 650 行
- [ ] `grep -r "/save" frontend/src/ --include="*.ts" --include="*.vue"` 确认无遗留调用
- [ ] ChatMessage 查询重复 ≤ 2 处
- [ ] stage_map 已替换为 RELATIONSHIP_STAGE_LABELS

---

## Phase 1.5 — TeacherPersona 合并到 Character（DD1）

> **前置条件**: Phase 1 已合并 | **预估工作量**: 1-2 天 | **风险**: 中高 | **PR 建议**: 独立 PR + 回滚脚本

### §1.5 数据模型迁移：删除 TeacherPersona（DD1 + DD12）

**⚠️ Reviewer 附条件要求**:
1. `is_active` 语义需文档化（建议：`Character.is_active` = "此角色可用于教学"）
2. Alembic 迁移必须独立 PR + 回滚脚本
3. 验收：`grep -r "TeacherPersona" backend/ --include="*.py"` 零结果

**操作**:

**步骤 1 — Character 模型变更**

在 `backend/models/models.py` 的 `Character` 类中新增列：
```python
traits = Column(JSON, nullable=True, comment='性格参数 5 维 {strictness, pace, questioning, warmth, humor}')
system_prompt_template = Column(Text, nullable=True, comment='自定义 system prompt 模板')
template_name = Column(String(50), nullable=True, comment='角色模板 key，如 socrates/einstein')
is_active = Column(Boolean, default=True, comment='是否可用于教学')
```

**步骤 2 — Alembic 迁移脚本**

```bash
cd backend && alembic revision --autogenerate -m "merge_teacher_persona_into_character"
```

迁移逻辑：
1. 从 `teacher_personas` 表读取数据
2. 将 `traits`、`system_prompt_template`、`is_active` 写入对应 `character_id` 的 Character 行
3. 将 `session.teacher_persona_id` FK 改为直接引用 `character_id`
4. DROP TABLE `teacher_personas`

**同时编写回滚脚本**：重建 teacher_personas 表并从 Character 迁回数据。

**步骤 3 — 代码修改**

1. **`backend/services/prompt_builder/builder.py`**:
   - `knowledge_svc` 参数重命名为 `db`（DD12）
   - 删除 `_get_character_from_persona()` 方法
   - `build_static_layer()` 直接从 Character 读取所有字段
   - 增加 template_name 感知（为 Phase 5 做准备）

2. **`backend/api/routes/learning.py`**:
   - `_get_session_characters()` 重写：直接查询 Character，不再 JOIN TeacherPersona
   - `_build_start_response()` 简化：从 Character 直接读取 traits

   3. **`backend/api/routes/archive.py`**（⚠️ 非 character.py，该文件不存在）:
      - 角色创建端点 `create_character()` 接受 `traits`、`system_prompt_template`、`template_name` 参数
      - 同时需删除 `_create_default_persona()` 函数（合并后此函数不再需要）
      - TeacherPersona CRUD 端点（`/teacher_persona` 系列共 5 个）需一并清理删除

**验收**:
- `alembic upgrade head` 成功
- `alembic downgrade -1` 成功（回滚验证）
- `pytest backend/tests/` 通过
- `grep -r "TeacherPersona" backend/ --include="*.py"` 零结果
- `grep -r "teacher_persona" backend/ --include="*.py"` 零结果（变量名也需更新）

### Phase 1.5 验收清单

- [ ] `alembic upgrade head` 成功
- [ ] `alembic downgrade -1` 成功
- [ ] `pytest backend/tests/` 全部通过
- [ ] `grep -r "TeacherPersona" backend/ --include="*.py"` 零结果
- [ ] `grep -r "knowledge_svc" backend/ --include="*.py"` 零结果（DD12 已修复）
- [ ] PromptBuilder 可直接从 Character 读取 traits 和 template_name
- [ ] `is_active` 语义已在代码注释中文档化

---

## Phase 2 — 前端清理（低风险）

> **前置条件**: Phase 1.5 已合并 | **预估工作量**: 0.5 天 | **风险**: 极低 | **PR 建议**: 单个 PR

### §2.1 删除 DEBUG 日志（#24）

**操作**:
```bash
grep -rn "\[DEBUG\]" frontend/src/ --include="*.ts" --include="*.vue"
```
删除所有 `console.log('[DEBUG]...`' 或 `console.debug('[DEBUG]...`' 行。预计 ~12 行。

### §2.2 删除冗余资源（#28）

**操作**:
1. 确认 `frontend/src/assets/home-bg.jpg` 是否被引用
2. 如无引用：删除文件
3. 保留 `home-bg.png`（如有引用）

### §2.3 删除 buildTraitsPayload 恒等函数（#16）

**文件**: 搜索 `frontend/src/` 中的 `buildTraitsPayload`

**操作**:
1. 确认函数实现为恒等变换（输入 === 输出）
2. 将所有调用点替换为直接传参
3. 删除函数定义

### §2.4 统一或删除 MOCK_CHARACTERS（#23）

**操作**:
1. `grep -rn "MOCK_CHARACTERS" frontend/src/ --include="*.ts" --include="*.vue"`
2. 如果仅用于开发调试：删除所有定义和引用
3. 如果有生产用途：合并为一份定义，放入 `constants/`

### §2.5 统一 page-fade CSS（#25）

**操作**:
1. `grep -rn "page-fade" frontend/src/ --include="*.css" --include="*.scss" --include="*.vue"`
2. 保留最完整的一份定义（建议放在 `frontend/src/styles/` 全局样式文件中）
3. 删除其他重复定义

### §2.6 简化 client.ts refresh 方法（#35）

**文件**: `frontend/src/api/client.ts`

**操作**:
1. 找到 refresh token 的响应处理
2. 删除 `data.data ?? data` 兼容代码（#213 修复后后端已统一返回格式）
3. 简化为直接使用 `data`

### Phase 2 验收清单

- [ ] `cd frontend && npm run build` 通过
- [ ] `grep -r "\[DEBUG\]" frontend/src/` 零结果
- [ ] `grep -r "buildTraitsPayload" frontend/src/` 零结果
- [ ] `grep -rc "page-fade" frontend/src/styles/` = 1

---

## Phase 3 — 前后端联调 + 角色系统统一

> **前置条件**: Phase 2 已合并 | **预估工作量**: 3-4 天 | **PR 建议**: 可拆为 3 个 PR（后端+前端常量+前端组件）

### §3.1 简化 update_learner_profile DB 连接（#18）

**文件**: `backend/services/user_profile.py`

**操作**:
1. 找到 `update_learner_profile` 函数
2. 移除 `db=None` fallback 参数
3. 改为通过 FastAPI `Depends(get_db)` 注入
4. 检查所有调用点是否已通过路由层传入 `db`

**验收**: 函数签名中无 `db=None`，`pytest` 通过

### §3.2 删除 tool_confirm 占位接口（#10）

**文件**: `backend/api/routes/learning.py`

**操作**:
1. 删除 `@router.post("/chat/tool_confirm")` 端点（L315-327）
2. 删除 `ToolConfirmRequest` 数据模型（在 models 或 routes 文件中搜索）
3. 确认前端无调用：`grep -r "tool_confirm" frontend/src/`

**验收**: `grep -r "tool_confirm" backend/api/ --include="*.py"` 零结果

### §3.3 统一 Limiter 实例（#02）

**文件**: `backend/api/routes/` 目录

**操作**:
1. 搜索所有 `Limiter` 或 `RateLimiter` 实例化
2. 提取到 `backend/core/config.py` 或新建 `backend/core/rate_limit.py`
3. 所有路由改为导入共享实例

### §3.4 补齐 useWorldStore 缺失方法（D2 前置）

**文件**: `frontend/src/stores/world.ts`

**需补齐的 5 个方法**（来自 characterApi）:

```typescript
// 在 useWorldStore 中添加：

async function getCharacter(id: number): Promise<Character> {
  const { data } = await client.get(`/character/${id}`)
  return data as Character
}

async function updateCharacter(id: number, payload: Partial<CharacterCreateRequest>): Promise<Character> {
  const { data } = await client.put(`/character/${id}`, payload)
  return data as Character
}

async function uploadAvatar(id: number, file: File): Promise<{ avatar: string }> {
  const formData = new FormData()
  formData.append('file', file)
  const { data } = await client.post(`/character/${id}/avatar`, formData, {
    headers: { 'Content-Type': 'multipart/form-data' },
  })
  return data as { avatar: string }
}

async function getCharacterStats() {
  const { data } = await client.get('/character/stats')
  return data
}

async function levelupCharacter(id: number, experiencePoints: number) {
  const { data } = await client.post(`/character/${id}/levelup`, { experience_points: experiencePoints })
  return data
}
```

**同时**: 在 return 语句中导出新方法。

### §3.5 删除 character_sprites legacy alias（#36）

**文件**: `backend/api/routes/learning.py` 或 `backend/services/`

**操作**:
1. 搜索 `_build_start_response` 函数
2. 找到 `character_sprites` 字段（是 `sage_sprites` 的别名）
3. 删除 `character_sprites` 行，仅保留 `sage_sprites`
4. 确认前端无引用：`grep -r "character_sprites" frontend/src/`

### §3.6 分离 types/index.ts 常量（#30）

**文件**: `frontend/src/types/index.ts`

**操作**:
1. 识别文件中的常量定义（非类型定义）
2. 创建 `frontend/src/constants/uiConstants.ts`
3. 迁移常量到新文件
4. 更新所有导入路径

### §3.7 角色系统统一（#31 + DD2/3/7/8/9/11）

> **整合设计层 DD2-DD11 的前端部分，与联调 §3.7 合并执行**

**决策汇总**:
- DD2: 统一到 SageCreateFlow，删除 CreatePersonaModal
- DD3: 模板 key 统一为 `socrates`
- DD7: TRAIT_SLIDERS 抽取到 `constants/traitSliders.ts`
- DD8: 删除 greeting（前端常量中的 greeting 也删除）
- DD9: 术语统一 — 字符串标签→`tags`，数值参数→`trait_params`
- DD11: 保留 5 个模板，每个需完整 `style_extensions`

**操作**:

**步骤 1 — 抽取 TRAIT_SLIDERS（DD7）**

1. 创建 `frontend/src/constants/traitSliders.ts`
2. 合并两个文件的 TRAIT_SLIDERS 为一份定义，保留详细版本（含 leftExample/rightExample）
3. 默认值统一：strictness=5, pace=5, questioning=5, warmth=6, humor=4

**步骤 2 — 统一术语（DD9）**

1. 全局搜索前端代码中模板相关的 `traits`（指字符串标签时）
2. 统一重命名为 `tags`
3. 数值参数相关统一重命名为 `trait_params`

**步骤 3 — 统一模板列表和 key（DD3 + DD11）**

1. 创建 `frontend/src/constants/characterTemplates.ts`（合并两个模板文件）
2. 仅保留 5 个模板：socrates、einstein、aristotle、sun_tzu、yoda
3. 所有模板 key 统一为 `socrates` 格式
4. 每个模板包含完整的 `style_extensions`（D4=C 风格指令占位）
5. 删除 `characterPresets.ts` 和 `personaTemplates.ts`

**步骤 4 — 统一到 SageCreateFlow（DD2 + DD8）**

1. 确认 SageCreateFlow 已引用新的 `characterTemplates.ts` 和 `traitSliders.ts`
2. 删除 `CreatePersonaModal.vue` 及相关文件
3. 删除前端常量中的 `greeting` 字段（DD8）
4. 更新所有引用：WorldDetail、Character 等页面
5. `speechStyles` 提交时 `join('、')`（DD6=B）

**验收**:
- `grep -r "socratic" frontend/src/` 零结果
- `grep -r "characterPresets" frontend/src/` 零结果
- `grep -r "personaTemplates" frontend/src/` 零结果
- `grep -r "CreatePersonaModal" frontend/src/` 零结果
- 每个模板都有 `style_extensions` 字段
- `npm run build` 通过

### §3.8 提取 useAsync + ErrorToast（#33）

**操作**:

1. 创建 `frontend/src/composables/useAsync.ts`:
```typescript
export function useAsync<T>(fn: () => Promise<T>) {
  const loading = ref(false)
  const error = ref<string | null>(null)
  // ... 通用 async/error 处理逻辑
}
```

2. 创建 `frontend/src/components/ErrorToast.vue`（全局组件）

3. 搜索所有 View 中的重复 try/catch + toast 模式
4. 替换为 `useAsync` + 全局 `ErrorToast`

### Phase 3 验收清单

- [ ] `pytest backend/tests/` 通过
- [ ] `cd frontend && npm run build` 通过（无 TS 错误）
- [ ] `grep -r "tool_confirm\|character_sprites" backend/ --include="*.py"` 零结果
- [ ] `grep -r "db=None" backend/services/ --include="*.py"` 零结果
- [ ] useWorldStore 包含 get/update/uploadAvatar/getStats/levelup 方法
- [ ] `grep -r "socratic" frontend/src/` 零结果（全部统一为 socrates）
- [ ] `grep -r "characterPresets\|personaTemplates\|CreatePersonaModal" frontend/src/` 零结果
- [ ] 角色创建端到端测试通过

---

## Phase 4 — 前端架构统一

> **前置条件**: Phase 3 已合并 | **预估工作量**: 2-3 天 | **风险**: 高 | **PR 建议**: 按子任务拆分为 3-4 个 PR

### §4.1 迁移 View 到 useWorldStore（#29, #32）

**决策**: Store 模式（D2）

**操作**:
1. 识别直接 `import client` 的 View 文件：
   ```bash
   grep -rl "import client from" frontend/src/views/
   ```
2. 逐个迁移：
   - `Worlds.vue` → 使用 `useWorldStore().fetchWorlds()`
   - `WorldDetail.vue` → 使用 `useWorldStore().selectWorld()` + `fetchCourses()`
   - `Character.vue` → 使用 `useWorldStore().fetchCharacters()`
3. 确认 `useWorldStore` 已包含所有需要的方法（§3.4 已补齐）
4. 删除或标记 `frontend/src/api/character.ts` 为已废弃

**验收**: `grep -r "import client" frontend/src/views/` 零结果

### §4.2 替换 alert() 为 toast（#27）

**操作**:
1. `grep -rn "alert(" frontend/src/views/ --include="*.vue"`
2. 替换为统一的 toast 调用（使用 Phase 3 创建的 ErrorToast 或现有 toast 组件）
3. 确保所有提示信息保留原有文案

### §4.3 统一背景/样式系统（#22 + DD10）

**操作**:
1. 搜索所有硬编码背景图片路径
2. 统一到 CSS 变量或配置
3. 建议在 `frontend/src/styles/variables.css` 中定义：
   ```css
   --bg-world-detail: url('@/assets/world-detail-bg.png');
   --bg-home: url('@/assets/home-bg.png');
   ```
4. 颜色系统统一为 hex（DD10）：在 `characterTemplates.ts` 中统一颜色格式

### §4.4 清理重复组件文件名（#26）

**操作**:
1. 列出 `frontend/src/components/` 根目录和 `galgame/` 子目录的同名文件
2. 评估是否可以合并或重命名
3. 更新所有导入路径

### Phase 4 验收清单

- [ ] 全部 e2e 测试通过：`cd frontend && npx playwright test`
- [ ] `grep -r "import client" frontend/src/views/` 零结果
- [ ] `grep -rn "alert(" frontend/src/views/` 零结果
- [ ] `npm run build` 通过，无 TS 错误
- [ ] 无重复组件文件名
- [ ] 颜色系统统一为 hex 格式

---

## Phase 5 — Prompt 生成策略优化

> **前置条件**: Phase 4 已合并 | **预估工作量**: 1-2 天 | **风险**: 中 | **PR 建议**: 单个 PR
> **关联决策**: DD4（基础规则+模板扩展）、DD5（template_name 持久化并影响 Prompt）

### §5.1 确认 template_name 已持久化（DD5）

**前置条件**: Phase 1.5 已在 Character 表添加 `template_name` 列。

**操作**:
1. 确认前端创建角色时传 `template_name` 字段
2. 确认后端角色创建/更新端点接收并存储 `template_name`
3. 确认已有角色（template_name=null）的降级行为正常

**验收**: 新创建的角色在数据库中有 template_name 值

### §5.2 实现 Prompt 基础规则+模板扩展（DD4）

**文件**: `backend/services/prompt_builder/builder.py`

**操作**:

1. **定义模板→风格指令映射**:

创建 `backend/services/prompt_builder/template_styles.py`:
```python
TEMPLATE_STYLE_EXTENSIONS = {
    'socrates': [
        '用反问法引导思考',
        '通过对话让对方发现自己逻辑中的矛盾',
    ],
    'einstein': [
        '多用类比和思想实验来解释概念',
        '鼓励学习者进行 gedankenexperiment（思维实验）',
    ],
    'aristotle': [
        '使用三段论和逻辑推理引导学习者',
        '从具体例子归纳到普遍原则',
    ],
    'sun_tzu': [
        '用战略比喻和战争案例来类比学习情境',
        '强调"知己知彼"式的自我认知',
    ],
    'yoda': [
        '用简洁有力的短句传递智慧',
        '通过倒装句式引发思考',
    ],
}

DEFAULT_STYLE = []  # template_name 为空时无扩展
```

2. **修改 PromptBuilder.build_static_layer()**:

在苏格拉底式基础规则之后，追加模板风格指令：
```python
# 基础规则（不变）
parts.append("【教学方法——苏格拉底式提问】...")

# 模板扩展（新增）
template_name = character.template_name
if template_name and template_name in TEMPLATE_STYLE_EXTENSIONS:
    extensions = TEMPLATE_STYLE_EXTENSIONS[template_name]
    parts.append("【角色风格指令】\n" + "\n".join(f"- {e}" for e in extensions))
```

**验收**:
- `pytest backend/tests/test_prompt_builder.py` 通过
- 不同模板角色的 system_prompt 有可观测的差异
- template_name 为 null 时，Prompt 与当前行为一致（无回归）

### Phase 5 验收清单

- [ ] `pytest backend/tests/` 通过
- [ ] `pytest backend/tests/test_prompt_builder.py` 通过
- [ ] 不同模板角色的 system_prompt 包含对应的风格指令
- [ ] template_name=null 的角色 Prompt 无变化（回归测试）

---

## Phase 6 — 子系统清理

> **前置条件**: Phase 5 已合并 | **预估工作量**: 2-3 天 | **风险**: 低-中 | **PR 建议**: 可拆为 2-3 个 PR

### §6.1 Seed Memory 语义文档化（DD14）

**文件**: `backend/services/learning_engine.py`

**操作**:
1. 在 `create_seed_memories()` 函数注释中明确："per-session seeding，同一 sage 不同 session 允许重复"
2. 在 `MemoryFact` 模型中添加 `source_session_id` 列（nullable），标记来源 session
3. `create_seed_memories()` 写入时填充 `source_session_id`

**验收**: seed memory 有 `source_session_id` 标记

### §6.2 修复记忆召回计数（DD15）

**文件**: `backend/services/memory_facts.py` + `backend/services/prompt_builder/`

**操作**:
1. 在 `MemoryFactsModule.format()` 检索记忆后，调用 `update_recall_count()`
2. 更新 `recall_count` 和 `last_recalled_at`

**验收**: 记忆检索后 `recall_count` 递增

### §6.3 统一 fact_type 枚举（DD16）

**文件**: `backend/services/memory_facts.py` + `backend/services/memory_extractor.py`

**操作**:
1. 在 `memory_facts.py` 中定义 Python Enum `FactType`
2. 删除 `memory_extractor.py` 中的硬编码 `valid_types` 集合
3. 改为从 `FactType` 枚举导入

**验收**: `grep -r "valid_types" backend/services/memory_extractor.py` 零结果

### §6.4 提取 Adapter 公共基类（DD17）

**文件**: `backend/services/llm/adapter.py`

**操作**:
1. 提取 `BaseOpenAIStyleAdapter` 基类
2. 公共逻辑：消息格式转换、SSE 流解析、错误处理
3. 4 个具体 Adapter 继承基类，仅保留差异化逻辑
4. ⚠️ **Reviewer 要求**: 先完成 DD1 迁移再处理此项

**验收**: `adapter.py` 减少 ~300 行，功能无变化

### §6.5 修复 chat_stream max_tokens（DD18）

**文件**: `backend/services/llm/adapter.py`

**操作**:
1. 将 `ClaudeAdapter.chat_stream()` 的 `"max_tokens": 1024` 改为 `self.get_model_info().max_tokens`
2. ⚠️ **需先与 Owner 确认**：修复不会降低流式体验，反而改善长回复截断问题

**验收**: 流式和非流式使用相同的 max_tokens

### §6.6 统一 ChatMessage 持久化职责（DD19）

**文件**: `backend/services/learning_engine.py` + `backend/api/routes/learning.py`

**操作**:
1. 将 ChatMessage 持久化逻辑从路由层移到 `learning_engine.process_message()` 内部
2. 更新路由层调用方式
3. 确保测试和 CLI 调用方也会持久化消息

**验收**: `learning_engine.process_message()` 返回结果已包含持久化的 ChatMessage

### §6.7 迁移 User Profile 端点（DD21）

**文件**: `backend/api/routes/learning.py` → 新建 `backend/api/routes/user.py`

**操作**:
1. 创建 `backend/api/routes/user.py`
2. 将 `GET /user/profile` 和 `POST /user/profile/refresh` 迁移过去
3. 在 `main.py` 中注册新路由
4. 从 `learning.py` 中删除

### §6.8 文档化 Chat 自动 start 副作用（DD22）

**文件**: `backend/api/routes/learning.py`

**操作**:
1. 在 `send_message()` 函数上方添加注释文档化自动 start 行为
2. 说明触发条件、副作用范围

### Phase 6 验收清单

- [ ] `pytest backend/tests/` 通过
- [ ] Seed memory 有 `source_session_id` 标记
- [ ] 记忆召回后 `recall_count` 正确递增
- [ ] `fact_type` 使用统一枚举
- [ ] `adapter.py` 代码量减少 ~300 行
- [ ] chat_stream max_tokens 与 chat 一致（需 Owner 确认）
- [ ] ChatMessage 持久化在 engine 内部完成

---

## Phase 7 — FSRS 接入（独立立项）

> **前置条件**: Phase 6 已合并 | **预估工作量**: 3-5 天 | **风险**: 高 | **独立立项**
> **关联决策**: DD20（掌握度占位符，高优先级但独立处理）

### §7.1 接入 FSRS 算法（DD20）

**现状**:
- `spaced_repetition.py` 和 FSRS 算法已存在但从未被学习引擎调用
- `learning_engine.py`: `"mastery_level": 50` 硬编码
- 前端: `masteryPercent.value += 3` 每条消息固定 +3%

**操作**:
1. 设计 FSRS 与 learning_engine 的集成方案（需独立设计文档）
2. 替换硬编码的 mastery_level
3. 前端对接真实掌握度数据
4. 评估对已有 session 数据的影响

**验收**: 掌握度基于 FSRS 算法动态计算

> ⚠️ 此 Phase 规模较大，建议联调完成后单独开启设计讨论。

---

## 执行总结

```
Phase 0 (0.5天) → Phase 1 (1-2天) → Phase 1.5 (1-2天) → Phase 2 (0.5天) → Phase 3 (3-4天)
  5 个问题          5 个问题          DD1+DD12            6 个问题          8+6 个问题
  死代码+bug       save.py 减肥      TeacherPersona       前端清理          联调+角色统一

→ Phase 4 (2-3天) → Phase 5 (1-2天) → Phase 6 (2-3天) → Phase 7 (3-5天)
    5 个问题           DD4+DD5           DD14-DD22          DD20
    架构统一           Prompt 优化        子系统清理          FSRS 接入
```

**总计**: 27 代码层 + 22 设计层 = **49 个待解决问题**，预计 **15-22 天**工作量。

每个 Phase 完成后需 Reviewer 审阅 PR，通过后方可进入下一 Phase。